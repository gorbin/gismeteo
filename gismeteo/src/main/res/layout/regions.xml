<LinearLayout
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        xmlns:android="http://schemas.android.com/apk/res/android"
        android:orientation="vertical"
        android:background="@color/gismeteo"
        android:paddingTop="@dimen/activity_vertical_margin"
        android:paddingBottom="@dimen/activity_vertical_margin"
        android:showDividers="none">

    <EditText
            android:id="@+id/inputSearch"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Search products.."
            android:inputType="textVisiblePassword"
            android:layout_marginTop="@dimen/edittext_top_margin"
            android:layout_marginLeft="@dimen/edittext_left_margin"
            android:layout_marginRight="@dimen/edittext_right_margin"
            android:layout_marginBottom="@dimen/edittext_bottom_margin"
        android:background="#ffffff" />
    <ListView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:id="@+id/region_list"
            android:choiceMode="singleChoice"
            android:layout_below="@+id/inputSearch"
            android:layout_alignParentLeft="true"
            android:background="@color/white"/>

    <ProgressBar
            style="?android:attr/progressBarStyleSmall"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:id="@+id/progressBar"
            android:layout_centerVertical="true"
            android:layout_centerHorizontal="true"/>

</LinearLayout>